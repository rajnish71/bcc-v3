---
/**
 * hub/index.astro — Member Hub Main Home (V6 09)
 * Route: /hub/
 *
 * Auth-gated via HubLayout. All data fetched client-side after auth check.
 *
 * Workspace sections (populated state):
 *   1. WelcomeBand        — time-aware greeting + single gold CTA (Upload)
 *   2. AttentionRow       — ≤3 computed items; hidden when empty
 *   3. RecentWork         — 5 photo mats + upload tile (or empty-state frame)
 *   4. HappeningAtTheClub — announcement card + event rows
 *   5. JourneyNumbers     — stats strip; hidden when all zero (day one)
 *   6. PersonalStack      — AcademyContinue + MembershipCard (2-col grid)
 *                           + JourneyStrip (full-width horizontal, populated only)
 *
 * Shell: 240px HubSidebar rail | fluid workspace (owned by HubLayout/HubSidebar)
 * Responsive: ≥1200 rail · 641–1199 HubTabs · ≤640 HubMobileBar
 *
 * PersonalStack lives INSIDE workspace below JourneyNumbers — not a right rail.
 * One gold CTA per view: Upload in WelcomeBand (populated) or empty-state frame (day one).
 * MEM-006: Constitutional class names ARE shown inside the hub (not a public surface).
 */
import HubLayout from '../../layouts/HubLayout.astro';
---

<HubLayout title="Member Hub" description="Your Bhopal Camera Club member dashboard.">

  <main class="hub-main" id="hub-main">

    <!-- ① WELCOME BAND ─────────────────────────────────────────────────── -->
    <div class="welcome-band">
      <div class="welcome-copy">
        <div class="section-kicker"><span class="kicker-rule"></span>Member Hub — Studio</div>
        <h1 class="welcome-greeting" id="hub-greeting">Welcome back</h1>
        <div class="welcome-meta" id="hub-meta">
          <span id="hub-class-label"></span>
          <span id="hub-since-text"></span>
          <a href="/photographers/" id="hub-portfolio-link" class="welcome-portfolio-link" hidden>
            View public portfolio ↗
          </a>
        </div>
      </div>
      <!-- Gold CTA: only shown when member has photographs (populated state) -->
      <a href="/hub/upload/" class="btn-gold-cta" id="hub-upload-cta" aria-label="Upload a Photograph" hidden>
        &#8593; Upload a Photograph
      </a>
    </div>

    <!-- ② ATTENTION ROW (hidden when no items) ──────────────────────────── -->
    <div class="attention-row" id="attention-row" hidden>
      <!-- items injected by JS: max 3 -->
    </div>

    <!-- ③a RECENT WORK (populated) ─────────────────────────────────────── -->
    <div class="section-block recent-work" id="recent-work" hidden>
      <div class="section-head">
        <div class="section-kicker"><span class="kicker-rule"></span>Your Latest Work</div>
        <a href="/hub/portfolio/" class="section-link">Portfolio &#8594;</a>
      </div>
      <!-- Loading: shimmer mats shown before API returns -->
      <div class="photo-grid" id="photo-grid-loading" aria-hidden="true">
        <div class="photo-mat photo-mat--shimmer"><div class="photo-thumb"></div><div class="shimmer-title"></div></div>
        <div class="photo-mat photo-mat--shimmer"><div class="photo-thumb"></div><div class="shimmer-title"></div></div>
        <div class="photo-mat photo-mat--shimmer"><div class="photo-thumb"></div><div class="shimmer-title"></div></div>
        <div class="photo-mat photo-mat--shimmer"><div class="photo-thumb"></div><div class="shimmer-title"></div></div>
        <div class="photo-mat photo-mat--shimmer"><div class="photo-thumb"></div><div class="shimmer-title"></div></div>
        <div class="upload-tile">
          <span class="upload-tile-plus">+</span>
          <span class="upload-tile-label">Add a photograph</span>
        </div>
      </div>
      <!-- Populated: real photo mats injected by JS -->
      <div class="photo-grid" id="photo-grid" hidden></div>
    </div>

    <!-- ③b EMPTY STATE — day one (gold CTA moves here) ─────────────────── -->
    <div class="empty-state-frame" id="empty-state" hidden>
      <h2 class="empty-title">Your portfolio begins here.</h2>
      <p class="empty-desc">Photographs you upload appear on your public portfolio page — and could hang on the club's wall.</p>
      <a href="/hub/upload/" class="btn-gold-cta">&#8593; Upload your first photograph</a>
    </div>

    <!-- ── HUB LOWER BAND ──────────────────────────────────────────────────
         Desktop: sections stacked in order (events → journey-numbers → personal)
         Tablet (641–1199px): events + personal side-by-side, journey-numbers below
    ────────────────────────────────────────────────────────────────────── -->
    <div class="hub-lower-band">

      <!-- ④ HAPPENING AT THE CLUB ──────────────────────────────────────── -->
      <div class="section-block hub-events-section" id="club-section" hidden>
        <div class="section-kicker"><span class="kicker-rule"></span>Happening at the Club</div>
        <div id="club-announcement" hidden></div>
        <div id="events-list"></div>
        <div id="events-loading" class="events-loading" aria-live="polite">
          <span class="hub-loading-dot"></span>
          <span class="hub-loading-dot"></span>
          <span class="hub-loading-dot"></span>
        </div>
      </div>

      <!-- ⑤ JOURNEY NUMBERS (hidden on day one — all stats zero) ──────── -->
      <div class="journey-numbers" id="journey-numbers" hidden>
        <div class="journey-stat">
          <div class="journey-num" id="jn-photos">—</div>
          <div class="journey-label">Photographs</div>
        </div>
        <div class="journey-stat">
          <div class="journey-num" id="jn-events">—</div>
          <div class="journey-label">Walks &amp; Tours</div>
        </div>
        <div class="journey-stat">
          <div class="journey-num" id="jn-courses">—</div>
          <div class="journey-label">Courses</div>
        </div>
        <div class="journey-stat">
          <div class="journey-num" id="jn-years">—</div>
          <div class="journey-label">Years</div>
        </div>
        <div class="journey-stat">
          <div class="journey-num" id="jn-award">—</div>
          <div class="journey-label">Award</div>
        </div>
      </div>

      <!-- ⑥ PERSONAL STACK ─────────────────────────────────────────────── -->
      <div class="personal-stack" id="personal-stack">

        <!-- 2-col grid: AcademyContinue | MembershipCard -->
        <div class="personal-stack-grid">

          <!-- AcademyContinue (populated: resume lesson; day one: browse Academy) -->
          <div class="personal-card academy-card" id="academy-card" hidden>
            <div class="personal-card__label" id="academy-card-label">Continue Learning</div>
            <div id="academy-card-body">
              <!-- injected by JS -->
            </div>
          </div>

          <!-- MembershipCard (dark) -->
          <div class="membership-dark-card" id="membership-dark-card" hidden>
            <img src="/brand/BCC White Logo.png" alt="BCC" class="mcard-logo" />
            <div class="mcard-name" id="mcard-name">—</div>
            <div class="mcard-class" id="mcard-class">Member</div>
            <div class="mcard-footer">
              <div>
                <div class="mcard-field-label">Membership No.</div>
                <div class="mcard-field-value" id="mcard-number">—</div>
              </div>
              <div class="mcard-expiry-col">
                <div class="mcard-field-label" id="mcard-expiry-label">Valid Thru</div>
                <div class="mcard-field-value mcard-field-value--gold" id="mcard-expiry">—</div>
              </div>
            </div>
            <div class="mcard-bottom">
              <a href="/hub/membership/" class="mcard-link">Membership &amp; Billing &#8594;</a>
              <span class="mcard-renews" id="mcard-renews" hidden></span>
            </div>
          </div>

        </div><!-- /personal-stack-grid -->

        <!-- JourneyStrip — horizontal 3-col, shown for established members only -->
        <div class="journey-strip" id="journey-card" hidden>
          <div class="personal-card__label">Your Journey</div>
          <div class="journey-milestones-grid" id="journey-milestones">
            <!-- injected by JS -->
          </div>
          <a href="/hub/journey/" class="journey-strip__link">Full journey &#8594;</a>
        </div>

      </div><!-- /personal-stack -->

    </div><!-- /hub-lower-band -->

  </main>

</HubLayout>

<style>
/* ═══════════════════════════════════════════════════════════════
   MAIN CONTENT COLUMN
═══════════════════════════════════════════════════════════════ */
.hub-main {
  padding-top: 40px;
  display: flex;
  flex-direction: column;
  gap: 0;
  min-width: 0;
}

/* ① WELCOME BAND ─────────────────────────────────────────────── */
.welcome-band {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 32px;
  padding-bottom: 24px;
  border-bottom: 1px solid #E7E1D6;
  flex-wrap: wrap;
}

.welcome-copy { flex: 1; min-width: 0; }

.section-kicker {
  display: flex;
  align-items: center;
  gap: 12px;
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: #A8843C;
  margin-bottom: 14px;
  font-family: var(--font-body);
}
.kicker-rule {
  display: inline-block;
  width: 28px;
  height: 1px;
  background: #C9A961;
  flex-shrink: 0;
}

.welcome-greeting {
  font-family: var(--font-display);
  font-size: clamp(32px, 4vw, 44px);
  font-weight: 700;
  letter-spacing: -0.025em;
  line-height: 1.05;
  color: #141210;
  margin: 0 0 10px;
}
.welcome-meta {
  font-size: 14px;
  color: #8B8378;
  display: flex;
  flex-wrap: wrap;
  gap: 0 6px;
  align-items: center;
  font-family: var(--font-body);
}
.welcome-portfolio-link {
  color: #A8843C;
  border-bottom: 1px solid rgba(168,132,60,0.4);
  text-decoration: none;
  transition: border-color 0.15s;
}
.welcome-portfolio-link:hover { border-color: #A8843C; }

/* Gold CTA — one per view */
.btn-gold-cta {
  display: inline-block;
  font-size: 15px;
  font-weight: 600;
  color: #FFFFFF;
  background: linear-gradient(135deg, #C9A961, #A8843C);
  padding: 13px 26px;
  border-radius: 2px;
  box-shadow: 0 2px 10px rgba(168,132,60,0.3), inset 0 1px 0 rgba(255,255,255,0.25);
  text-decoration: none;
  white-space: nowrap;
  flex-shrink: 0;
  transition: box-shadow 0.2s, transform 0.15s;
  font-family: var(--font-body);
}
.btn-gold-cta:hover {
  box-shadow: 0 4px 16px rgba(168,132,60,0.4), inset 0 1px 0 rgba(255,255,255,0.25);
  transform: translateY(-1px);
}

/* ② ATTENTION ROW ────────────────────────────────────────────── */
.attention-row {
  display: flex;
  flex-wrap: wrap;
  border-bottom: 1px solid #E7E1D6;
  margin-bottom: 36px;
}
.attention-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 16px 24px 16px 0;
  font-size: 13px;
  font-weight: 500;
  color: #141210;
  white-space: nowrap;
  text-decoration: none;
  border-right: 1px solid #F0EBE0;
  margin-right: 24px;
  font-family: var(--font-body);
}
.attention-item:last-child { border-right: none; margin-right: 0; }
.attention-dot {
  width: 7px; height: 7px;
  border-radius: 999px;
  background: linear-gradient(135deg, #C9A961, #A8843C);
  flex-shrink: 0;
}

/* ③ SECTION BLOCK / RECENT WORK ──────────────────────────────── */
.section-block { margin-bottom: 40px; }
.recent-work   { margin-bottom: 40px; }

.section-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 18px;
}
.section-link {
  font-size: 13px;
  font-weight: 500;
  color: #A8843C;
  border-bottom: 1px solid rgba(168,132,60,0.4);
  text-decoration: none;
  font-family: var(--font-body);
}

/* Photo grid */
.photo-grid {
  display: grid;
  grid-template-columns: repeat(6, 1fr);
  gap: 14px;
}
@media (min-width: 641px) and (max-width: 1199px) { .photo-grid { grid-template-columns: repeat(4, 1fr); } }
@media (max-width: 640px)                          { .photo-grid { grid-template-columns: repeat(3, 1fr); } }

.photo-mat {
  background: #FFFFFF;
  border: 1px solid #E7E1D6;
  box-shadow: 0 1px 2px rgba(20,18,16,0.04);
  padding: 8px;
  text-decoration: none;
  color: inherit;
  display: block;
  transition: box-shadow 0.15s;
}
.photo-mat:hover { box-shadow: 0 4px 12px rgba(20,18,16,0.1); }

.photo-thumb {
  aspect-ratio: 4 / 5;
  background: #1A1816;
  overflow: hidden;
  position: relative;
}
.photo-thumb img {
  width: 100%; height: 100%;
  object-fit: cover;
  display: block;
}

.photo-thumb-empty {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 4px;
}

.photo-mat-meta {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 4px;
  margin-top: 8px;
}
.photo-mat-title {
  font-size: 11px;
  font-weight: 600;
  color: #141210;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  font-family: var(--font-body);
}
.photo-vis-badge {
  font-size: 9px;
  font-weight: 600;
  letter-spacing: 0.08em;
  padding: 2px 5px;
  flex-shrink: 0;
  font-family: var(--font-body);
}
.photo-vis-badge--public  { background: #E8EFE9; color: #3E5A48; }
.photo-vis-badge--pending { background: #FBF0E8; color: #8C6830; }
.photo-vis-badge--private { background: #F4F0E8; color: #57514B; border: 1px solid #D8D2C6; }

/* Photo loading shimmer */
.photo-mat--shimmer { pointer-events: none; }
.photo-mat--shimmer .photo-thumb {
  background: linear-gradient(90deg, #E7E1D6 25%, #F4F0E8 50%, #E7E1D6 75%);
  background-size: 200% 100%;
  animation: hub-shimmer 1.5s ease-in-out infinite;
}
.shimmer-title {
  height: 10px;
  width: 65%;
  background: linear-gradient(90deg, #E7E1D6 25%, #F4F0E8 50%, #E7E1D6 75%);
  background-size: 200% 100%;
  animation: hub-shimmer 1.5s ease-in-out infinite;
  animation-delay: 0.1s;
  margin-top: 10px;
  border-radius: 1px;
}
@keyframes hub-shimmer {
  0%   { background-position:  200% 0; }
  100% { background-position: -200% 0; }
}

.upload-tile {
  padding: 8px;
  border: 1px dashed #C9A961;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8px;
  text-decoration: none;
  color: #A8843C;
  transition: background 0.15s;
  min-height: 80px;
}
.upload-tile:hover { background: rgba(201,169,97,0.06); }
.upload-tile-plus  { font-size: 20px; line-height: 1; }
.upload-tile-label { font-size: 11px; font-weight: 500; color: #8B8378; text-align: center; line-height: 1.3; font-family: var(--font-body); }

/* ③b EMPTY STATE ─────────────────────────────────────────────── */
.empty-state-frame {
  border: 1px dashed #C9A961;
  padding: 56px 48px;
  text-align: center;
  margin-bottom: 36px;
}
.empty-title {
  font-family: var(--font-display);
  font-size: 24px;
  font-weight: 600;
  letter-spacing: -0.01em;
  color: #141210;
  margin: 0 0 10px;
}
.empty-desc {
  font-size: 14px;
  color: #8B8378;
  max-width: 400px;
  margin: 0 auto 24px;
  line-height: 1.7;
  font-family: var(--font-body);
}

/* ── HUB LOWER BAND ─────────────────────────────────────────────
   Desktop:        flex column — events → journey-numbers → personal
   Tablet 641–1199: grid — events | personal / journey (full-width)
   Mobile ≤640:    flex column
────────────────────────────────────────────────────────────────── */
.hub-lower-band {
  display: flex;
  flex-direction: column;
}

@media (min-width: 641px) and (max-width: 1199px) {
  .hub-lower-band {
    display: grid;
    grid-template-areas:
      "events  personal"
      "journey journey";
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    align-items: start;
  }
  .hub-events-section { grid-area: events;   margin-bottom: 0; }
  .journey-numbers    { grid-area: journey;  margin-bottom: 0; }
  .personal-stack     { grid-area: personal; margin-top: 0; }
}

/* ④ HAPPENING AT THE CLUB ────────────────────────────────────── */
.club-announcement {
  background: #F4F0E8;
  border: 1px solid #E7E1D6;
  padding: 18px 22px;
  margin-bottom: 16px;
}
.announcement-label {
  font-size: 10px;
  font-weight: 600;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: #8B8378;
  margin-bottom: 8px;
  font-family: var(--font-body);
}
.announcement-title {
  font-family: var(--font-display);
  font-weight: 600;
  font-size: 16px;
  color: #141210;
  margin: 0 0 5px;
}
.announcement-sub {
  font-size: 13px;
  color: #57514B;
  margin: 0;
  font-family: var(--font-body);
}
.announcement-link { color: #A8843C; }

/* Event rows */
.event-row {
  display: grid;
  grid-template-columns: 56px 80px 1fr auto;
  gap: 18px;
  align-items: center;
  padding: 16px 0;
  border-bottom: 1px solid #F0EBE0;
  font-family: var(--font-body);
}
.event-row:last-child { border-bottom: none; }

/* No-thumb variant: empty state events and tablet/mobile */
.event-row--no-thumb {
  grid-template-columns: 56px 1fr auto;
}
.event-row--no-thumb .event-thumb { display: none; }

@media (max-width: 640px) {
  .event-row           { grid-template-columns: 44px 1fr; gap: 12px; }
  .event-thumb         { display: none; }
  .event-action-register,
  .event-action-registered { display: none; }
}

.event-date-col { text-align: center; }
.event-day {
  font-family: var(--font-display);
  font-size: 24px;
  font-weight: 700;
  line-height: 1;
  color: #141210;
}
.event-month {
  font-family: var(--font-mono);
  font-size: 9px;
  color: #8B8378;
  margin-top: 3px;
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

.event-thumb {
  aspect-ratio: 3 / 2;
  background: #1A1816;
  overflow: hidden;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.event-tags {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 5px;
}
.event-type-tag {
  font-size: 9px;
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  padding: 3px 8px;
  font-family: var(--font-body);
}
.event-type-tag--walk     { background: #E8EFE9; color: #3E5A48; }
.event-type-tag--workshop { background: #F5EDD8; color: #8C6830; }
.event-type-tag--tour     { background: #EAE8F2; color: #4A4870; }
.event-type-tag--meetup   { background: #F4F0E8; color: #57514B; border: 1px solid #D8D2C6; }

.event-time {
  font-family: var(--font-mono);
  font-size: 10px;
  color: #8B8378;
}
.event-title {
  font-family: var(--font-display);
  font-weight: 600;
  font-size: 16px;
  color: #141210;
  margin: 0 0 3px;
}
.event-location { font-size: 11px; color: #8B8378; }

.event-action-registered {
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.06em;
  color: #3E5A48;
  background: #E8EFE9;
  padding: 6px 12px;
  white-space: nowrap;
  font-family: var(--font-body);
}
.event-action-register {
  font-size: 13px;
  font-weight: 600;
  color: #141210;
  border: 1px solid #D8D2C6;
  padding: 8px 16px;
  border-radius: 2px;
  white-space: nowrap;
  text-decoration: none;
  font-family: var(--font-body);
  transition: border-color 0.15s;
}
.event-action-register:hover { border-color: #141210; }

.events-loading {
  display: flex;
  gap: 5px;
  align-items: center;
  justify-content: center;
  padding: 24px 0;
}
.hub-loading-dot {
  width: 6px; height: 6px;
  background: #D8D2C6;
  border-radius: 999px;
  animation: hub-pulse 1.2s ease-in-out infinite both;
}
.hub-loading-dot:nth-child(2) { animation-delay: 0.2s; }
.hub-loading-dot:nth-child(3) { animation-delay: 0.4s; }
@keyframes hub-pulse {
  0%, 80%, 100% { transform: scale(0.6); opacity: 0.4; }
  40%            { transform: scale(1);   opacity: 1; }
}

/* ⑤ JOURNEY NUMBERS ──────────────────────────────────────────── */
.journey-numbers {
  border-top: 1px solid #E7E1D6;
  border-bottom: 1px solid #E7E1D6;
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  margin-bottom: 40px;
}
.journey-stat {
  padding: 18px 0;
  text-align: center;
  border-right: 1px solid #F0EBE0;
}
.journey-stat:last-child { border-right: none; }
.journey-num {
  font-family: var(--font-mono);
  font-size: 22px;
  color: #141210;
  line-height: 1;
}
.journey-label {
  font-size: 10px;
  color: #8B8378;
  margin-top: 4px;
  letter-spacing: 0.06em;
  font-family: var(--font-body);
}

@media (min-width: 641px) and (max-width: 1199px) {
  /* Tablet: show 4 stats, hide Award column */
  .journey-numbers { grid-template-columns: repeat(4, 1fr); }
  .journey-stat:last-child { display: none; }
}
@media (max-width: 640px) {
  /* Mobile: show 3 stats */
  .journey-numbers {
    grid-template-columns: repeat(3, 1fr);
    background: #FFFFFF;
    border: 1px solid #E7E1D6;
    margin-bottom: 20px;
  }
  .journey-stat:nth-child(4),
  .journey-stat:nth-child(5) { display: none; }
  .journey-num { font-size: 16px; }
}

/* ⑥ PERSONAL STACK ───────────────────────────────────────────── */
.personal-stack { margin-top: 40px; }

.personal-stack-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24px;
}

@media (min-width: 641px) and (max-width: 1199px) {
  /* Tablet: personal is a single column (stacked cards, right of events) */
  .personal-stack-grid { grid-template-columns: 1fr; }
}

@media (max-width: 640px) {
  .personal-stack      { margin-top: 20px; }
  .personal-stack-grid { grid-template-columns: 1fr; }
}

/* Personal card (Academy wrapper) */
.personal-card {
  background: #FFFFFF;
  border: 1px solid #E7E1D6;
  box-shadow: 0 1px 2px rgba(20,18,16,0.04);
  padding: 22px;
}
.personal-card__label {
  font-size: 10px;
  font-weight: 600;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: #A8843C;
  margin-bottom: 10px;
  font-family: var(--font-body);
}

/* Academy card inner */
.academy-course-title {
  font-family: var(--font-display);
  font-weight: 600;
  font-size: 17px;
  line-height: 1.25;
  color: #141210;
  margin: 0 0 4px;
}
.academy-lesson {
  font-family: var(--font-mono);
  font-size: 10px;
  color: #8B8378;
  margin-bottom: 14px;
}
.academy-progress-track {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 16px;
}
.academy-progress-bar  { flex: 1; height: 2px; background: #E7E1D6; }
.academy-progress-fill { height: 2px; background: linear-gradient(90deg, #C9A961, #A8843C); }
.academy-progress-pct  { font-family: var(--font-mono); font-size: 10px; color: #A8843C; }
.academy-resume-btn {
  display: inline-block;
  font-size: 12px;
  font-weight: 600;
  color: #A8843C;
  border: 1px solid #C9A961;
  background: rgba(201,169,97,0.08);
  padding: 9px 16px;
  border-radius: 2px;
  text-decoration: none;
  font-family: var(--font-body);
  transition: background 0.15s;
}
.academy-resume-btn:hover { background: rgba(201,169,97,0.15); }

.academy-browse-title {
  font-family: var(--font-display);
  font-weight: 600;
  font-size: 17px;
  color: #141210;
  margin: 0 0 8px;
}
.academy-browse-desc {
  font-size: 13px;
  color: #57514B;
  line-height: 1.65;
  font-family: var(--font-body);
}
.academy-browse-link { color: #A8843C; }

/* Membership dark card */
.membership-dark-card {
  background: #141210;
  padding: 22px;
  color: #FAF8F4;
}
.mcard-logo   { height: 24px; width: auto; margin-bottom: 16px; }
.mcard-name {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 17px;
  color: #FAF8F4;
  margin: 0;
}
.mcard-class {
  font-size: 10px;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  color: #C9A961;
  margin-top: 4px;
  font-family: var(--font-body);
}
.mcard-footer {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  margin-top: 16px;
  padding-top: 14px;
  border-top: 1px solid rgba(250,248,244,0.12);
}
.mcard-expiry-col { text-align: right; }
.mcard-field-label {
  font-family: var(--font-mono);
  font-size: 9px;
  color: rgba(250,248,244,0.45);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}
.mcard-field-value {
  font-family: var(--font-mono);
  font-size: 12px;
  color: #FAF8F4;
  margin-top: 3px;
}
.mcard-field-value--gold { color: #C9A961; }
.mcard-bottom {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 14px;
  padding-top: 12px;
  border-top: 1px solid rgba(250,248,244,0.12);
  font-size: 11px;
  color: rgba(250,248,244,0.5);
  font-family: var(--font-body);
}
.mcard-link { color: rgba(250,248,244,0.5); text-decoration: none; }
.mcard-link:hover { color: #C9A961; }
.mcard-renews {
  font-size: 9px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: #C9A961;
  font-family: var(--font-body);
}

/* Journey Strip — horizontal 3-col inside personal-stack */
.journey-strip {
  background: #FFFFFF;
  border: 1px solid #E7E1D6;
  box-shadow: 0 1px 2px rgba(20,18,16,0.04);
  padding: 22px;
  margin-top: 24px;
}
.journey-milestones-grid {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 0;
}
.journey-milestone {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px 24px 10px 0;
  border-right: 1px solid #F4F0E8;
}
.journey-milestone + .journey-milestone { padding-left: 24px; }
.journey-milestone:last-child { border-right: none; padding-right: 0; }

.journey-strip__link {
  font-size: 11px;
  color: #A8843C;
  margin-top: 12px;
  padding-top: 12px;
  border-top: 1px solid #F4F0E8;
  display: block;
  text-decoration: none;
  font-family: var(--font-body);
}

/* Hide Journey Strip at tablet and mobile — shown desktop only */
@media (max-width: 1199px) { .journey-strip { display: none; } }

.milestone-icon {
  width: 28px; height: 28px;
  border-radius: 999px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.milestone-icon--gold    { background: linear-gradient(135deg, #C9A961, #A8843C); }
.milestone-icon--neutral { background: #F4F0E8; }

.milestone-title {
  font-size: 12px;
  font-weight: 600;
  color: #141210;
  margin: 0;
  font-family: var(--font-body);
}
.milestone-sub {
  font-family: var(--font-mono);
  font-size: 9px;
  color: #8B8378;
  margin-top: 1px;
}

/* SVG placeholder stroke */
.ph-svg { stroke: #3E3A36; }

/* ═══════════════════════════════════════════════════════════════
   MOBILE OVERRIDES
═══════════════════════════════════════════════════════════════ */
@media (max-width: 640px) {
  .hub-main          { padding-top: 20px; }
  .welcome-band      { gap: 16px; }
  .welcome-greeting  { font-size: 28px; }
  .empty-state-frame { padding: 40px 24px; }
  .empty-title       { font-size: 20px; }
  .section-kicker    { font-size: 10px; }
}

/* ═══════════════════════════════════════════════════════════════
   TABLET OVERRIDES
═══════════════════════════════════════════════════════════════ */
@media (min-width: 641px) and (max-width: 1199px) {
  .hub-main { padding-top: 24px; }
}
</style>

<script>
import { logout } from '../../lib/auth';

const API = '/api/v1';

/* ── Utilities ──────────────────────────────────────────────── */
function tok(): string { return localStorage.getItem('bcc_token') ?? ''; }

async function apiFetch(path: string): Promise<Response> {
  return fetch(path, { headers: { Authorization: `Bearer ${tok()}` } });
}

function show(id: string) {
  const el = document.getElementById(id);
  if (el) el.hidden = false;
}
function hide(id: string) {
  const el = document.getElementById(id);
  if (el) el.hidden = true;
}
function setText(id: string, text: string) {
  const el = document.getElementById(id);
  if (el) el.textContent = text;
}

/* Time-of-day greeting */
function greeting(firstName: string): string {
  const h = new Date().getHours();
  if (h < 12) return `Good morning, ${firstName}.`;
  if (h < 17) return `Good afternoon, ${firstName}.`;
  return `Good evening, ${firstName}.`;
}

/* Map class_id → display label (MEM-006: shown inside hub, not public surface) */
const CLASS_LABELS: Record<number, string> = {
  1: 'Founding Member',
  2: 'Patron Member',
  3: 'Life Member',
  4: 'Full Member',
  5: 'Basic Member',
  6: 'Student Member',
  7: 'Individual Member',
};

/* Photo visibility badge */
function visBadge(vis: string): string {
  if (vis === 'PUBLIC')       return '<span class="photo-vis-badge photo-vis-badge--public">PUBLIC</span>';
  if (vis === 'MEMBERS_ONLY') return '<span class="photo-vis-badge photo-vis-badge--pending">MEMBERS</span>';
  return '<span class="photo-vis-badge photo-vis-badge--private">PRIVATE</span>';
}

/* Placeholder SVG for photos without thumbnails */
const PHOTO_PH = `
  <div class="photo-thumb-empty">
    <svg width="28" height="28" viewBox="0 0 28 28" fill="none" class="ph-svg">
      <rect x="2" y="2" width="24" height="24" stroke-width="1"/>
      <line x1="2" y1="26" x2="26" y2="2" stroke-width="0.5"/>
      <circle cx="9" cy="8" r="2.5" stroke-width="0.75"/>
    </svg>
  </div>`;

/* Event type → tag CSS class + label */
function eventTypeTag(type: string): string {
  const map: Record<string, [string, string]> = {
    'PHOTO_WALK': ['walk',     'Photo Walk'],
    'WORKSHOP':   ['workshop', 'Workshop'],
    'TOUR':       ['tour',     'Tour'],
    'MEETUP':     ['meetup',   'Meetup'],
  };
  const [cls, label] = map[type?.toUpperCase()] ?? ['meetup', type ?? 'Event'];
  return `<span class="event-type-tag event-type-tag--${cls}">${label}</span>`;
}

/* ── LOAD USER ──────────────────────────────────────────────── */
async function loadUser(): Promise<{ firstName: string; createdAt: string; userId: number; username: string } | null> {
  const res = await apiFetch(`${API}/users/me`);
  if (!res.ok) return null;
  const u = await res.json() as { fullName?: string; createdAt?: string; id?: number; username?: string };

  const firstName = (u.fullName ?? 'there').trim().split(/\s+/)[0];
  const createdAt = u.createdAt ?? '';
  const userId    = u.id ?? 0;
  const username  = u.username ?? '';

  setText('hub-greeting', greeting(firstName));

  const sinceYear = createdAt ? new Date(createdAt).getFullYear() : null;
  const sinceEl = document.getElementById('hub-since-text');
  if (sinceEl) sinceEl.textContent = sinceYear ? `member since ${sinceYear}` : '';

  const portfolioLink = document.getElementById('hub-portfolio-link') as HTMLAnchorElement | null;
  if (portfolioLink && username) {
    portfolioLink.href   = `/photographers/${username}/`;
    portfolioLink.hidden = false;
  }

  return { firstName, createdAt, userId, username };
}

/* ── LOAD MEMBERSHIP ────────────────────────────────────────── */
async function loadMembership(): Promise<void> {
  const res = await apiFetch(`${API}/membership/mine`);
  if (!res.ok) {
    show('membership-dark-card');
    setText('mcard-name', '—');
    return;
  }

  const memberships = await res.json() as Array<{
    lifecycle_state?: string;
    membership_number?: string | null;
    membership_class_id?: number | null;
    expires_at?: string | null;
    activated_at?: string | null;
  }>;

  const active = memberships.find(m => m.lifecycle_state === 'ACTIVE')
    || memberships.find(m => m.lifecycle_state === 'APPROVED')
    || memberships.find(m => m.lifecycle_state === 'PENDING')
    || memberships[0];

  if (!active) return;

  const classLabel = active.membership_class_id
    ? (CLASS_LABELS[active.membership_class_id] ?? 'Member')
    : 'Member';

  // Welcome meta class label
  const classEl = document.getElementById('hub-class-label');
  if (classEl) classEl.textContent = classLabel + ' ·';

  // Dark membership card
  show('membership-dark-card');
  setText('mcard-class', classLabel.toUpperCase());

  const numEl = document.getElementById('mcard-number');
  if (numEl) numEl.textContent = active.membership_number ?? 'Pending';

  // Expiry / renewal
  const expiryEl      = document.getElementById('mcard-expiry');
  const expiryLabelEl = document.getElementById('mcard-expiry-label');
  if (active.expires_at) {
    const d = new Date(active.expires_at);
    if (expiryEl) expiryEl.textContent = d.toLocaleDateString('en-IN', { month: 'short', year: 'numeric' }).toUpperCase();
    if (expiryLabelEl) expiryLabelEl.textContent = 'Valid Thru';

    const daysLeft = Math.ceil((d.getTime() - Date.now()) / 86400000);
    if (daysLeft <= 60 && daysLeft > 0) {
      const renewEl = document.getElementById('mcard-renews');
      if (renewEl) { renewEl.textContent = `RENEWS ${daysLeft}D`; renewEl.hidden = false; }
      addAttentionItem(
        `Membership renews <span style="font-family:var(--font-mono);margin-left:4px">${d.toLocaleDateString('en-IN',{day:'numeric',month:'short'})}</span>`,
        '/hub/membership/'
      );
    }
  } else {
    if (expiryEl) expiryEl.textContent = 'Lifetime';
    if (expiryLabelEl) expiryLabelEl.textContent = 'Status';
  }

  // Populate member name from localStorage (fast path)
  const raw = localStorage.getItem('bcc_user');
  if (raw) {
    try {
      const u = JSON.parse(raw) as { displayName?: string };
      if (u.displayName) setText('mcard-name', u.displayName);
    } catch { /* ignore */ }
  }
}

/* ── ATTENTION ROW helpers ──────────────────────────────────── */
const attentionItems: Array<{ html: string; href: string }> = [];

function addAttentionItem(html: string, href: string) {
  attentionItems.push({ html, href });
}

function renderAttentionRow() {
  if (!attentionItems.length) return;
  const row = document.getElementById('attention-row');
  if (!row) return;
  row.innerHTML = attentionItems.slice(0, 3).map(item =>
    `<a href="${item.href}" class="attention-item">
       <span class="attention-dot"></span>${item.html} &rarr;
     </a>`
  ).join('');
  row.hidden = false;
}

/* ── LOAD PHOTOS ────────────────────────────────────────────── */
async function loadPhotos(): Promise<number> {
  const res = await apiFetch(`${API}/gallery/photos?limit=6`);
  if (!res.ok) {
    // On error: hide shimmer, show empty state
    hide('photo-grid-loading');
    show('empty-state');
    return 0;
  }

  const data = await res.json() as {
    photos: Array<{
      uuid: string;
      title?: string | null;
      visibility?: string;
      urls?: { thumbnail?: string; medium?: string };
    }>;
    total: number;
  };

  const photos = data.photos ?? [];
  const total  = data.total ?? photos.length;

  if (photos.length === 0) {
    // Day one: hide shimmer section, show empty-state with gold CTA
    hide('recent-work');
    show('empty-state');
    return 0;
  }

  // Populated: replace shimmer with real mats
  hide('photo-grid-loading');
  show('hub-upload-cta');
  show('recent-work');

  const grid = document.getElementById('photo-grid');
  if (!grid) return total;

  const mats = photos.slice(0, 5).map(p => {
    const thumbUrl   = p.urls?.thumbnail ?? p.urls?.medium ?? '';
    const thumbInner = thumbUrl
      ? `<img src="${thumbUrl}" alt="${p.title ?? 'Photograph'}" loading="lazy" />`
      : PHOTO_PH;
    const title = p.title ?? 'Untitled';
    const vis   = visBadge(p.visibility ?? 'PRIVATE');
    return `
      <a href="/hub/photo/${p.uuid}/" class="photo-mat" title="${title}">
        <div class="photo-thumb">${thumbInner}</div>
        <div class="photo-mat-meta">
          <div class="photo-mat-title">${title}</div>
          ${vis}
        </div>
      </a>`;
  }).join('');

  const uploadTile = `
    <a href="/hub/upload/" class="upload-tile" aria-label="Upload a photograph">
      <span class="upload-tile-plus">+</span>
      <span class="upload-tile-label">Add a photograph</span>
    </a>`;

  grid.innerHTML = mats + uploadTile;
  grid.hidden = false;

  setText('jn-photos', String(total));

  return total;
}

/* ── LOAD EVENTS ────────────────────────────────────────────── */
async function loadEvents(isEmpty: boolean): Promise<void> {
  show('club-section');

  const res = await fetch(`${API}/events?upcoming=true&limit=3`);
  hide('events-loading');

  if (!res.ok) return;

  const events = await res.json() as Array<{
    id: number;
    title?: string;
    event_type?: string;
    starts_at?: string;
    location_name?: string | null;
    location_address?: string | null;
  }>;

  const listEl = document.getElementById('events-list');
  if (!listEl || !events.length) return;

  // Attention: upcoming event
  if (events[0]) {
    const first = events[0];
    const d = first.starts_at ? new Date(first.starts_at) : null;
    if (d) {
      const tomorrow = new Date(); tomorrow.setDate(tomorrow.getDate() + 1);
      const isToday    = d.toDateString() === new Date().toDateString();
      const isTomorrow = d.toDateString() === tomorrow.toDateString();
      const when = isToday ? 'today' : isTomorrow ? 'tomorrow' : d.toLocaleDateString('en-IN', { day: 'numeric', month: 'short' });
      addAttentionItem(
        `${first.title ?? 'Upcoming event'} registration opens <span style="font-family:var(--font-mono);margin-left:4px">${when}</span>`,
        `/activities/${first.id}/`
      );
    }
  }

  listEl.innerHTML = events.map(ev => {
    const d    = ev.starts_at ? new Date(ev.starts_at) : null;
    const day  = d ? String(d.getDate()).padStart(2, '0') : '--';
    const mon  = d ? d.toLocaleString('en-IN', { month: 'short' }).toUpperCase() : '';
    const time = d ? d.toLocaleTimeString('en-IN', { hour: '2-digit', minute: '2-digit', hour12: true }) : '';
    const loc  = [ev.location_name, ev.location_address].filter(Boolean).join(', ');

    if (isEmpty) {
      // Day one: simplified 3-col row (no event thumbnail)
      return `
        <div class="event-row event-row--no-thumb">
          <div class="event-date-col">
            <div class="event-day">${day}</div>
            <div class="event-month">${mon}</div>
          </div>
          <div class="event-info">
            <div class="event-title">${ev.title ?? 'Event'}</div>
            <div class="event-location">${ev.event_type ?? ''} · ${time}${loc ? ' · ' + loc : ''}</div>
          </div>
          <a href="/activities/${ev.id}/" class="event-action-register">Register</a>
        </div>`;
    }

    return `
      <div class="event-row">
        <div class="event-date-col">
          <div class="event-day">${day}</div>
          <div class="event-month">${mon}</div>
        </div>
        <div class="event-thumb">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none" class="ph-svg">
            <rect x="1" y="1" width="18" height="18" stroke-width="0.75"/>
            <line x1="1" y1="19" x2="19" y2="1" stroke-width="0.5"/>
          </svg>
        </div>
        <div class="event-info">
          <div class="event-tags">${eventTypeTag(ev.event_type ?? '')}<span class="event-time">${time}</span></div>
          <div class="event-title">${ev.title ?? 'Event'}</div>
          ${loc ? `<div class="event-location">${loc}</div>` : ''}
        </div>
        <a href="/activities/${ev.id}/" class="event-action-register">Register</a>
      </div>`;
  }).join('');
}

/* ── ACADEMY CARD ───────────────────────────────────────────── */
function renderAcademyCard(hasPhotos: boolean) {
  show('academy-card');
  const labelEl = document.getElementById('academy-card-label');
  const bodyEl  = document.getElementById('academy-card-body');
  if (!bodyEl) return;

  if (hasPhotos) {
    if (labelEl) labelEl.textContent = 'Continue Learning';
    bodyEl.innerHTML = `
      <div class="academy-course-title">Foundations of Exposure</div>
      <div class="academy-lesson">Lesson 5 of 12 — Metering modes</div>
      <div class="academy-progress-track">
        <div class="academy-progress-bar"><div class="academy-progress-fill" style="width:42%"></div></div>
        <span class="academy-progress-pct">42%</span>
      </div>
      <a href="/hub/academy/" class="academy-resume-btn">Resume lesson &rarr;</a>`;
  } else {
    if (labelEl) labelEl.textContent = 'BCC Academy';
    bodyEl.innerHTML = `
      <div class="academy-browse-title">Start learning</div>
      <p class="academy-browse-desc">Structured paths from exposure fundamentals to genre mastery.<br>
        <a href="/hub/academy/" class="academy-browse-link">Browse the Academy &rarr;</a>
      </p>`;
  }
}

/* ── JOURNEY STRIP (horizontal 3-col, populated members only) ── */
function renderJourneyStrip() {
  const milestonesEl = document.getElementById('journey-milestones');
  if (!milestonesEl) return;

  const milestones = [
    { gold: true,  title: 'First Award',    sub: 'Monsoon Frames 2024' },
    { gold: false, title: '10th Walk',       sub: 'Chowk Bazaar · Mar 2025' },
    { gold: false, title: '40 Photographs',  sub: 'Gallery milestone · Jan 2025' },
  ];

  milestonesEl.innerHTML = milestones.map(m => `
    <div class="journey-milestone">
      <div class="milestone-icon ${m.gold ? 'milestone-icon--gold' : 'milestone-icon--neutral'}">
        ${m.gold
          ? `<svg width="12" height="12" viewBox="0 0 12 12" fill="none"><path d="M6 1l1.5 3H11L8.5 6.5l1 3L6 8 2.5 9.5l1-3L1 4h3.5z" fill="white"/></svg>`
          : `<svg width="12" height="12" viewBox="0 0 12 12" fill="none"><rect x="1" y="1" width="10" height="10" stroke="#8B8378" stroke-width="1"/><line x1="1" y1="11" x2="11" y2="1" stroke="#8B8378" stroke-width="0.75"/></svg>`
        }
      </div>
      <div>
        <div class="milestone-title">${m.title}</div>
        <div class="milestone-sub">${m.sub}</div>
      </div>
    </div>`).join('');

  show('journey-card');
}

/* ── JOURNEY NUMBERS ────────────────────────────────────────── */
function renderJourneyNumbers(photoCount: number) {
  if (photoCount === 0) return;
  setText('jn-photos', String(photoCount));
  setText('jn-events', '—');
  setText('jn-courses', '—');
  setText('jn-years', '—');
  setText('jn-award', '—');
  show('journey-numbers');
}

/* ── BOOTSTRAP ──────────────────────────────────────────────── */
async function init() {
  // Show recent-work section immediately (shimmer mats visible)
  show('recent-work');

  const [userResult, photoCount] = await Promise.all([
    loadUser().catch(() => null),
    loadPhotos().catch(() => 0),
  ]);

  const hasPhotos = photoCount > 0;

  await Promise.all([
    loadMembership().catch(() => {}),
    loadEvents(!hasPhotos).catch(() => {}),
  ]);

  renderAttentionRow();
  renderAcademyCard(hasPhotos);
  if (hasPhotos) {
    renderJourneyStrip();
    renderJourneyNumbers(photoCount);
  }

  // Sync membership card name from localStorage
  if (userResult) {
    const raw = localStorage.getItem('bcc_user');
    if (raw) {
      try {
        const u = JSON.parse(raw) as { displayName?: string };
        if (u.displayName) setText('mcard-name', u.displayName);
      } catch { /* ignore */ }
    }
  }
}

init().catch(() => {});
</script>
